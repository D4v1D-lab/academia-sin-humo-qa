{
  "name": "academia-sin-humo-qa",
  "version": "1.0.0",
  "private": true,
  "description": "Proyecto final: suite de QA automation sobre Academia sin Humo (playground) — Playwright + TypeScript",
  "scripts": {
    "test": "playwright test --project=chromium",
    "test:e2e": "playwright test tests/e2e --project=chromium",
    "test:api": "playwright test tests/api --project=chromium",
    "test:integrado": "playwright test tests/integrado --project=chromium",
    "test:a11y": "playwright test tests/a11y --project=chromium",
    "report": "playwright show-report"
  },
  "license": "ISC",
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.55.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
